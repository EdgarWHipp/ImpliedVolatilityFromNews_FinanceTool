# Copy to .env and fill in. .env is gitignored.
FMP_API_KEY=your_fmp_key_here
# Free tier: 250 calls/day, NO transcript endpoint (needs Starter+).
# We respect ~5 calls/sec to stay safe.

GEMINI_API_KEY=
